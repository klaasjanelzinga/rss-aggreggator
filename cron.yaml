cron:
  - description: "refresh data on venues"
    url: /maintenance/fetch-data
    schedule: every 24 hours
  - description: "clean old events"
    url: /maintenance/cleanup
    schedule: every 4 hours
