cron:
  - description: "refresh data on spot-groningen"
    url: /maintenance/fetch-data?venue_id=spot-groningen
    schedule: every 24 hours
  - description: "clean old events"
    url: /maintenance/cleanup
    schedule: every 24 hours
